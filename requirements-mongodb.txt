# MongoDB variant. Djongo pins its own supported stack: it requires
# Django <= 3.1.12, pymongo <= 3.11.4, and sqlparse == 0.2.4. This backend
# therefore runs on an older Django than the PostgreSQL variant, even though
# the shared source tree was scaffolded with Django 4.2.
Django==3.1.12
djongo==1.3.7
pymongo==3.11.4
sqlparse==0.2.4
